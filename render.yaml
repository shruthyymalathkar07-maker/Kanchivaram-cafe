services:
  - type: web
    name: kanchivaram-cafe-backend
    env: node
    plan: free
    buildCommand: npm install && npx prisma db push --schema=server/prisma/schema.prisma && npx tsx server/prisma/seed.ts
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        sync: false
